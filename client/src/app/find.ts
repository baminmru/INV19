import { enums } from './enums';

export namespace find { 

 export interface   FILTER { // Запись
	thePart:string; // Деталь
	dstart:string;  // Начало интервала
	dend:string;  // Конец интервала
 }
}